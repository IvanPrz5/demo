package com.example.demo.Service;

import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.example.demo.Entity.c_Pais;
import com.example.demo.Repository.c_PaisRepository;

@Service
public class c_PaisService {
    /* @Autowired
    c_PaisRepository paisRepository;
    public List<c_Pais> getPaisAndDescripcion(String cPais, String descripcion){
        return paisRepository.findByPaisAndDescripcion(cPais, descripcion);
    }     */
}
